{
    "name": "servera",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "esbuild server.ts --bundle --outfile=../../dist/server.js",
        "dev": "nodemon --watch server.ts --exec ts-node server.ts"
    },
    "devDependencies": {
        "typescript": "^5.3.3"
    },
    "dependencies": {
        "esbuild": "^0.19.5"
    }
}